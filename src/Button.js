import React, { Component } from 'react';
import './App.css';

class Button extends Component {
  render() {
    return (
      <button
      className={'button'}
      type='button'>
        Get started
      </button>
    );
  }
}

export default Button;
